match /users/{userId} {
  allow write: if request.auth != null && request.auth.uid == userId 
    && request.resource.data.keys().hasAll(['level', 'xp'])
    && request.resource.data.level is int
    && request.resource.data.xp is number
    && request.resource.data.level >= 0;
}