name: Auto-Deploy Firestore Rules

on:
  push:
    branches:
      - main
    paths:
      - 'firestore.rules'

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: 📥 Descargar código
        uses: actions/checkout@v4

      - name: ⚙️ Instalar Firebase CLI
        run: npm install -g firebase-tools

      - name: 🚀 Desplegar Reglas en Producción
        env:
          FIREBASE_CREDENTIALS: ${{ secrets.FIREBASE_CREDENTIALS }}
          PROJECT_ID: ${{ secrets.FIREBASE_PROJECT_ID }}
        run: |
          echo "$FIREBASE_CREDENTIALS" > firebase_key.json
          echo '{"firestore": {"rules": "firestore.rules"}}' > firebase.json
          export GOOGLE_APPLICATION_CREDENTIALS="$(pwd)/firebase_key.json"
          firebase deploy --only firestore:rules --project $PROJECT_ID
